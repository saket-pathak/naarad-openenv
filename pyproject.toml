[project]
name = "naarad-openenv"
version = "0.1.0"
description = "AI-powered grievance assistant"
authors = [
    { name="Saket", email="your-email@example.com" }
]
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"